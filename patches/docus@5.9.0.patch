diff --git a/app/pages/[[lang]]/[...slug].vue b/app/pages/[[lang]]/[...slug].vue
index 0f0c11c4db4acefe1cd349b8a5dd99c9ceabf7af..6bf8ed523d859403641d33ff35ae4d97af787219 100644
--- a/app/pages/[[lang]]/[...slug].vue
+++ b/app/pages/[[lang]]/[...slug].vue
@@ -49,6 +49,7 @@ defineOgImage('Docs', {
   headline: headline.value,
   title: title?.slice(0, 60),
   description: formatOgDescription(title, description),
+  fontFamily: 'Poppins',
 })
 
 const github = computed(() => appConfig.github ? appConfig.github : null)
diff --git a/app/templates/landing.vue b/app/templates/landing.vue
index 160ee000099448105fe396eff1140d53b6ae1982..d34bf3220161d83b670bc6d7f777a3aa95c5187e 100644
--- a/app/templates/landing.vue
+++ b/app/templates/landing.vue
@@ -26,6 +26,7 @@ if (!page.value?.seo?.ogImage) {
   defineOgImage('Landing', {
     title: title?.slice(0, 60),
     description: formatOgDescription(title, description),
+    fontFamily: 'Poppins',
   })
 }
 </script>
diff --git a/i18n/locales/nl.json b/i18n/locales/nl.json
index f72dff99fa71300e2fa10726523197fcc57158a5..1098e9a464ab4b4551ce04c4cf1375bdd1d0a2af 100644
--- a/i18n/locales/nl.json
+++ b/i18n/locales/nl.json
@@ -32,5 +32,32 @@
     "wordmarkDownloaded": "Woordmerk gedownload",
     "copyLogoFailed": "Logo kopiëren mislukt",
     "copyWordmarkFailed": "Woordmerk kopiëren mislukt"
+  },
+  "assistant": {
+    "title": "Vraag AI",
+    "placeholder": "Stel een vraag...",
+    "tooltip": "Stel een vraag aan AI",
+    "tryAsking": "Probeer een vraag te stellen",
+    "askAnything": "Vraag maar raak...",
+    "clearChat": "Chat wissen",
+    "close": "Sluiten",
+    "expand": "Uitvouwen",
+    "collapse": "Inklappen",
+    "thinking": "Aan het nadenken...",
+    "askMeAnything": "Vraag maar raak",
+    "askMeAnythingDescription": "Krijg hulp bij het navigeren door de documentatie, het begrijpen van concepten en het vinden van antwoorden.",
+    "faq": "Veelgestelde vragen",
+    "chatCleared": "De chat wordt gewist bij verversen",
+    "lineBreak": "Regelafbreking",
+    "explainWithAi": "Uitleggen met AI",
+    "toolListPages": "Vermelde documentatiepagina's",
+    "toolReadPage": "Lezen",
+    "loading": {
+      "searching": "De documentatie doorzoeken",
+      "reading": "De documentatie doornemen",
+      "analyzing": "De inhoud analyseren",
+      "finding": "Het beste antwoord vinden",
+      "finished": "Gebruikte bronnen"
+    }
   }
 }
diff --git a/modules/assistant/runtime/server/api/search.ts b/modules/assistant/runtime/server/api/search.ts
index 5b61d864f6414b79178cd25f9ffc9e5110dec62d..f4c54fb02e53f61f82fca4910e5d847701882a3c 100644
--- a/modules/assistant/runtime/server/api/search.ts
+++ b/modules/assistant/runtime/server/api/search.ts
@@ -4,6 +4,21 @@ import { createMCPClient } from '@ai-sdk/mcp'
 
 const MAX_STEPS = 10
 
+function createSafeFetch(): typeof fetch {
+  const boundFetch = fetch.bind(globalThis)
+
+  return (input, init) => {
+    const headers = new Headers(init?.headers)
+
+    headers.set('accept', 'application/json, text/event-stream')
+
+    return boundFetch(input, {
+      ...init,
+      headers,
+    })
+  }
+}
+
 // eslint-disable-next-line @typescript-eslint/no-explicit-any
 function stopWhenResponseComplete({ steps }: { steps: any[] }): boolean {
   const lastStep = steps.at(-1)
@@ -73,7 +88,7 @@ export default defineEventHandler(async (event) => {
       : `${getRequestURL(event).origin}${baseURL}${mcpServer}`
 
   const httpClient = await createMCPClient({
-    transport: { type: 'http', url: mcpUrl },
+    transport: { type: 'http', url: mcpUrl, fetch: createSafeFetch() },
   })
   const mcpTools = await httpClient.tools()
 
diff --git a/modules/config.ts b/modules/config.ts
index 1140c3b14e6103f8b9dc168aab9a2a9c7b3baca8..93975578a33c3428fa5524dda6cd88c8a2a00c0a 100644
--- a/modules/config.ts
+++ b/modules/config.ts
@@ -21,12 +21,13 @@ export default defineNuxtModule({
   async setup(_options, nuxt) {
     const dir = nuxt.options.rootDir
     const url = inferSiteURL()
+    const siteUrl = typeof url === 'string' ? url : undefined
     const meta = await getPackageJsonMetadata(dir)
     const gitInfo = await getLocalGitInfo(dir) || getGitEnv()
     const siteName = (typeof nuxt.options.site === 'object' && nuxt.options.site?.name) || meta.name || gitInfo?.name || ''
 
     nuxt.options.llms = defu(nuxt.options.llms, {
-      domain: url,
+      domain: siteUrl,
       title: siteName,
       description: meta.description || '',
       full: {
@@ -36,10 +37,10 @@ export default defineNuxtModule({
     })
 
     nuxt.options.site = defu(nuxt.options.site, {
-      url,
+      url: siteUrl,
       name: siteName,
       debug: false,
-    })
+    }) as any
 
     nuxt.options.appConfig.header = defu(nuxt.options.appConfig.header, {
       title: siteName,
diff --git a/nuxt.config.ts b/nuxt.config.ts
index 5a5267dcacc1e3bb7225a6002f4cdac1dff88899..9f1230f1ce009d0db77b01eef425e4139081bb9b 100644
--- a/nuxt.config.ts
+++ b/nuxt.config.ts
@@ -1,5 +1,6 @@
 import { join } from 'node:path'
 import { extendViteConfig, createResolver, useNuxt } from '@nuxt/kit'
+import { defineNuxtConfig } from 'nuxt/config'
 
 const { resolve } = createResolver(import.meta.url)
 
@@ -13,7 +14,7 @@ export default defineNuxtConfig({
     resolve('./modules/skills'),
     resolve('./modules/css'),
     () => {
-      const nuxt = useNuxt()
+      const nuxt = useNuxt() as any
       nuxt.options.icon ||= {}
       nuxt.options.icon.customCollections ||= []
       nuxt.options.icon.customCollections.push({
@@ -89,8 +90,8 @@ export default defineNuxtConfig({
     },
   },
   hooks: {
-    'nitro:config'(nitroConfig) {
-      const nuxt = useNuxt()
+    'nitro:config'(nitroConfig: any) {
+      const nuxt = useNuxt() as any
 
       const i18nOptions = (nuxt.options as typeof nuxt.options & { i18n?: DocusI18nOptions }).i18n
 
diff --git a/server/routes/sitemap.xml.ts b/server/routes/sitemap.xml.ts
index 92978caba9589206229f6a91723ece256e2fb6d8..a73a5a21c5974e0b283ee8a332cb9213488be255 100644
--- a/server/routes/sitemap.xml.ts
+++ b/server/routes/sitemap.xml.ts
@@ -13,15 +13,8 @@ export default defineEventHandler(async (event) => {
 
   const availableLocales = getAvailableLocales(config.public as Record<string, unknown>)
   const collections = getCollectionsToQuery(undefined, availableLocales)
-
-  if (availableLocales.length > 0) {
-    for (const locale of availableLocales) {
-      collections.push(`landing_${locale}`)
-    }
-  }
-  else {
-    collections.push('landing')
-  }
+  // Docus routing can disable landing collections when app/pages/index.vue exists.
+  // Query only docs collections here to avoid prerender failures on missing landing collections.
 
   const urls: SitemapUrl[] = []
 
